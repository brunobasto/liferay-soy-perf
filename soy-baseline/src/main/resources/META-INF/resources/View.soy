{namespace View}

/**
 * Prints the Soy Baseline portlet main view.
 *
 * @param id
 */
{template .render}
	<div id="{$id}">
		<p>
			<b>{msg desc=""}soy_baseline_SoyBaseline.caption{/msg}</b>
		</p>
	</div>
{/template}